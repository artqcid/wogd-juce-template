# WOGD JUCE Template - Environment Variables Example
# Copy this file to .env and adjust paths for your system

# JUCE Installation (Option 1: Central installation)
# Set this to use a shared JUCE installation across projects
# WOGD_JUCE_DIR=C:/dev/juce-8.0.4

# CLAP JUCE Extensions (Optional)
# Set this if you have a local clap-juce-extensions repository
# CLAP_JUCE_EXTENSIONS_DIR=C:/dev/clap-juce-extensions

# WebView2 SDK (Optional)
# Set this if you have a local WebView2 SDK installation
# WEBVIEW2_SDK_DIR=C:/dev/webview2-sdk

# AudioPluginHost for Debugging (Optional)
# Set this to enable plugin debugging with JUCE AudioPluginHost
# JUCE_AUDIO_PLUGIN_HOST=C:/path/to/JUCE/extras/AudioPluginHost/Builds/VisualStudio2026/x64/Release/App/AudioPluginHost.exe

# Note: If these are not set, the build system will:
# - Look for JUCE as a git submodule
# - Download dependencies automatically via CPM
# - Use default build configurations
