{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Plugin (AudioPluginHost)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${env:JUCE_AUDIO_PLUGIN_HOST}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
            "description": "Launch plugin with AudioPluginHost. Set JUCE_AUDIO_PLUGIN_HOST environment variable to your AudioPluginHost.exe path."
        }
    ]
}